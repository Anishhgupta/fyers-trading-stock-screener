# Core
python-dotenv>=1.0.0
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3

# Dashboard
streamlit>=1.32

# Broker SDKs (install whichever broker you'll actually use)
fyers-apiv3>=3.1.0        # for Fyers
smartapi-python>=1.4.0    # for Angel One
logzero>=1.7.0            # smartapi-python dependency
pyotp>=2.9.0
# websocket-client is a dependency of fyers-apiv3 / smartapi-python.
# Deliberately NOT pinned here -- pinning it caused a resolver conflict
# between the two SDKs' own version ranges. Let pip resolve it from
# whichever broker package(s) you actually install below.

# Packaging into a standalone .exe deliverable
pyinstaller>=6.5.0

# Testing
pytest>=8.0
